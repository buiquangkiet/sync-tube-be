# sync-tube-be

Backend SyncTube — Spring Boot + MySQL + WebSocket + OAuth.

Deploy: [RAILWAY.md](./RAILWAY.md)

## Local (Docker)

```bash
cp .env.example .env
docker compose up -d --build
```

API: `http://localhost:8081`
